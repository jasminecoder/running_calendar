---
name: backend-models
description: Write Rails ActiveRecord models with proper validations, associations, scopes, and callbacks. Use when creating or editing model files, defining validations, setting up has_many/belongs_to associations, writing scopes, or working with Active Storage attachments in app/models/ files.
---

## When to use this skill

- When creating or editing Rails model files (`app/models/*.rb`)
- When adding validations (presence, uniqueness, inclusion, format, custom)
- When defining associations (belongs_to, has_many, has_one, has_one_attached)
- When writing scopes for common queries
- When adding callbacks (before_save, after_create, etc.)
- When implementing instance methods on models
- When setting up Active Storage file attachments
- When defining enums or attribute accessors

# Backend Models

This Skill provides Claude Code with specific guidance on how to write Rails models following project conventions.

## Instructions

For details, refer to the information provided in this file:
[Rails Model Standards](../../../agent-os/standards/backend/models.md)
