---
name: global-validation
description: Implement Rails validations at model and database levels with strong parameters and proper error display. Use when adding model validations, creating database constraints in migrations, using strong parameters in controllers, displaying form errors, or sanitizing user input in Rails applications.
---

## When to use this skill

- When adding validations to Rails models (`validates :field, presence: true`)
- When creating database constraints in migrations (`null: false`, `unique: true`)
- When implementing custom validation methods
- When using strong parameters to whitelist input
- When displaying validation errors in forms
- When validating inclusion in allowed values
- When adding format validations with regex
- When implementing conditional validations

# Global Validation

This Skill provides Claude Code with specific guidance on Rails validation patterns.

## Instructions

For details, refer to the information provided in this file:
[Rails Validation Standards](../../../agent-os/standards/global/validation.md)
