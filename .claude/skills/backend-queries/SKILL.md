---
name: backend-queries
description: Write efficient ActiveRecord queries using eager loading, scopes, and proper indexing strategies. Use when writing database queries, fixing N+1 problems, using includes/joins/preload, wrapping operations in transactions, or optimizing query performance in Rails models and controllers.
---

## When to use this skill

- When writing ActiveRecord queries in models or controllers
- When fixing N+1 query problems using `includes`, `preload`, or `joins`
- When selecting specific columns with `select` or `pluck`
- When wrapping related database operations in transactions
- When optimizing slow queries
- When deciding which columns to index
- When using `where`, `order`, `limit`, `offset` methods
- When writing complex queries with multiple conditions

# Backend Queries

This Skill provides Claude Code with specific guidance on how to write efficient ActiveRecord queries following project conventions.

## Instructions

For details, refer to the information provided in this file:
[ActiveRecord Query Standards](../../../agent-os/standards/backend/queries.md)
